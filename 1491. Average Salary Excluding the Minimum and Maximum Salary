class Solution(object):
    def average(self, salary):
        """
        :type salary: List[int]
        :rtype: float
        """
        salary.sort()
        salary.pop(0)
        salary.pop()
        return sum(salary)/float(len(salary))
        
