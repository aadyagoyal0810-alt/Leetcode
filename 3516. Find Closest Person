class Solution(object):
    def findClosest(self, x, y, z):
        """
        :type x: int
        :type y: int
        :type z: int
        :rtype: int
        """
        import math
        a=fabs(x-z)
        b=fabs(y-z)
        if(a>b):
            return 2
        elif(b>a):
            return 1
        else:
            return 0
